"use client";

// Re-export the existing thread detail page.
// The channel layout wraps this with a compressed thread list (3-column mode).
export { default } from "@/app/(app)/thread/[threadId]/page";
